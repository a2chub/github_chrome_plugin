# src/

ソースコードを格納するディレクトリです。

## ディレクトリ構成

- `background/`: Background Script (Service Worker) のコード
- `content/`: Content Script のコード
- `options/`: Options Page（設定画面）のコード
- `types/`: TypeScript型定義
- `utils/`: ユーティリティ関数

## ビルド

このディレクトリ内のファイルは、ビルドツールによって `dist/` ディレクトリにビルドされます。

